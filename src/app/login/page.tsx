import LoginForm from "@/components/auth/login-form";

export default function LoginPage() {
  return (
    <div className="w-full h-full max-w-80 flex items-center justify-center">
      <LoginForm />
    </div>
  );
}
